package com.oceancode.cloud.test.ui.container;

import com.microsoft.playwright.Locator;

public class Card extends UIContainer{
    public Card(UIContainer parent, Locator locator) {
        super(parent, locator);
    }
}
